/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.imgur.com'], // Replace 'example.com' with the domain you want to allow
      },
}

module.exports = nextConfig
